Place the wedding BGM file here as `bgm.mp3` (referenced by `invitationData.bgm.src` in
`src/config/invitationData.ts`). Until a real file is added, the AudioToggle button will
fail to play silently and visually disable itself.
